import 'package:flutter/material.dart';

class FindSreen extends StatelessWidget {
  const FindSreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: new AppBar(
        title: new Text('发现'),
      ),
      body: Center(
        child: new Text('发现')
      )
    );
  }
}
